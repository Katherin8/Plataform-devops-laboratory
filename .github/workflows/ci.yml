name: CI Pipeline Laboratory

on:
   push:
   pull_request:

jobs:
    build: Machine
      runs-on: ubuntu-latest
    
      steps:
        - name: Checkout Repository
          uses: actions/checkout@v4
           
        - name: Execute Image en Makefile
          run: make ci
